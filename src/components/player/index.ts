export { default } from './Player';

// @ts-expect-error TS(6142): Module './PlayerDialog' was resolved to '/Users/ya... Remove this comment to see the full error message
export { default as PlayerDialog } from './PlayerDialog';
